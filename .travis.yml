language: python
python:
  - "3.6"
addons:
  sonarcloud:
    organization: "cdot-cv"
    token: ${SONAR_TOKEN}


script:
  - coverage erase
  - coverage run -m pytest 'translator/source code/translator.py'
  - coverage xml -i
  - sonar-scanner

